import { ReactNode } from "react";

export default function LayoutApplicationManagement({ children }: { children: ReactNode }) {
    return (
        <div>
            {children}
        </div>
    );
}
